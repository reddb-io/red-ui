# Changesets

This repo follows the same release shape as `reddb`: public npm packages live
under the `@reddb-io/*` scope and are versioned together.

Use:

```sh
pnpm changeset
pnpm release:version
pnpm release:publish
```
