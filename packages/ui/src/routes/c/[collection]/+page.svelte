<!--
  URL marker only. The Mountable Root (mounted in +layout.svelte) reads the
  collection from the Kit router and opens/focuses its tab, so this page
  intentionally renders nothing of its own.
-->
