<!--
  URL marker only. The Mountable Root (mounted in +layout.svelte) reads the
  location from the Kit router and renders the settings view itself, so this
  page intentionally renders nothing of its own.
-->
